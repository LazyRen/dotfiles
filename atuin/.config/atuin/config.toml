search_mode = "daemon-fuzzy"
enter_accept = false
style = "compact"
inline_height = 20

[daemon]
enabled = true
autostart = true

[keys]
prefix = 't'

[sync]
records = true

[ui]
columns = ["command"]
